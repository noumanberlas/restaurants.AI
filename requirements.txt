# ── Microsoft Agent Framework ──────────────────────────────────────────────
# Core + Foundry + OpenAI-compatible Ollama + orchestration integrations
# These published versions are mutually compatible.
agent-framework-core==1.16.0
agent-framework-foundry==1.11.0
agent-framework-openai==1.14.1
agent-framework-orchestrations==1.1.1

# Azure identity (Foundry auth via DefaultAzureCredential / AzureCliCredential)
azure-identity>=1.17.0

# Foundry projects SDK (toolboxes, publishing prompt agents)
azure-ai-projects>=2.1.0

# Environment & config
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP server (hosts agents as FastAPI services)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data / persistence stubs (swap out as needed)
sqlalchemy>=2.0.0

# Observability & tracing
azure-monitor-opentelemetry>=1.6.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Debugging (required by VS Code Foundry Toolkit Agent Inspector)
debugpy

